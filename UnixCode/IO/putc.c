/*
    #include <stdio.h>

    // 函数实现，可以作为参数
    int fputc(int c, FILE *stream);
        argument:
            - c: 待写入字符
            - stream: 文件对象指针

        return:
            - 成功返回 c
            - 失败返回 EOF

    // 可以通过宏实现，不能取地址
    int putc(int c, FILE *stream);

    // putc(c, stdout);
    int putchar(int c);

 */