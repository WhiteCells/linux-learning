/*
    #include <stdio.h>

    // ISO C 标准 I/O 库提供了创建临时文件的函数
    // 产生一个与现有文件名不同的一个有效路径名字符串
    // 每次调用产生一个不同路径名
    // 最多调用 TMP_MAX 次，定义在 stdio.h 中
    char *tmpnam(char *s);
        argument:
            - s:

        return:
            - 

    // 
    FILE *tmpfile(void);
 */