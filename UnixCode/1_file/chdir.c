/*
    #include <unistd.h>

    // 更改当前工作目录
    int chdir(const char *path);
        argument:
            - path:

        return:
            - 成功返回 0
            - 失败返回 -1，设置 errno
    int fchdir(int fd);
        argument:
            - fd: 文件描述符
 */


#include <unistd.h>
#include <stdio.h>

int main() {

    return 0;
}