/*
    // 当前进程发送信号
    #include <signal.h>

    int raise(int sig);
        argument:
            - sig: 发送的信号的编号或宏值
            
        return: 
            - 0: success
            - !0: failure
 */

#include <signal.h>
#include <stdio.h>

int main() {

    return 0;
}